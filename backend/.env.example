# === CONFIGURACIÓN DE BASE DE DATOS ===
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=tu_contraseña  # Deja vacío si no tiene contraseña
DB_NAME=artesanos_dev
DB_PORT=3306

# === CONFIGURACIÓN DE AUTENTICACIÓN ===
JWT_SECRET=tu_clave_secreta_muy_larga_y_compleja
JWT_EXPIRE=30m
RESET_TOKEN_EXPIRE=30m

# === ENTORNO ===
NODE_ENV=development
FRONTEND_URL=http://localhost:3000